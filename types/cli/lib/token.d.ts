import { optionsCreateKeys } from "../../dist/lib/token";

interface tokenAnotherback {
    name: string;
    key: string;
    options: optionsCreateKeys;
}

export default function token(arr: Array<tokenAnotherback>): void;